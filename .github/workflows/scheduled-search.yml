name: Scheduled Search

on:
  schedule:
    # GitHub Actions cron uses UTC. 06:15 UTC is usually 07:15 or 08:15 in Europe/Copenhagen depending on DST.
    - cron: "15 6 * * 1"
    - cron: "15 6 * * 4"

permissions:
  contents: write

jobs:
  search:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Run search pipeline
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          npm run search
          npm run report
          npm run site-data

      - name: Commit generated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data public/data public/screenshots
          git diff --cached --quiet || git commit -m "chore: refresh holiday data"
          git push

